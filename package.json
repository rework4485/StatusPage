{
  "name": "status-page",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.73.0"
  },
  "scripts": {
    "postinstall": "bun add -g wrangler",
    "deploy": "wrangler pages deploy . --project-name status-page"
  }
}
