{
  "name": "status-page",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.73.0"
  },
  "scripts": {
    "postinstall": "ln -sf \"$(pwd)/node_modules/.bin/wrangler\" /usr/local/bin/wrangler",
    "deploy": "wrangler pages deploy . --project-name status-page"
  }
}
