{
  "name": "status-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build":     "node scripts/build.js",
    "predeploy": "node scripts/build.js",
    "deploy":    "vercel --prod"
  }
}
